import styled from 'styled-components';

export const Wrapper = styled.div `
    height: 57px;
    box-shadow: 7px 1px 8px rgba(136, 165, 191, 0.15);
    background-color: #ffffff;

    header {
        background-color: #ffffff;
   }

    button {
        color: #062A3D;
        font-weight: 400;
        font-size: 14px;
        line-height: 19px;
        text-transform: none;
    }
    
   
`;

export const LogoImage = styled.div `
    margin: 0 100px 0 10px;
    
    img {
    margin-right: -28px;
    }
`;